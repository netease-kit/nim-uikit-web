<view wx:if="{{visible}}" class="preview-image-container" bindtap="handleClose">
  <view class="preview-image-wrapper">
    <image 
      src="{{imageUrl}}" 
      class="preview-image" 
      mode="aspectFit"
      catchtap="preventClose"
      binderror="onImageError"
      bindload="onImageLoad"
    />
    <view class="close-button" bindtap="handleClose">×</view>
  </view>
</view>