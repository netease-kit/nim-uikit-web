<view class="icon-wrapper {{iconClassName}}" style="{{iconStyle}}">
  <image 
    src="{{_url}}" 
    style="width: {{computedWidth}}px; height: {{computedHeight}}px;" 
    class="icon"
    mode="aspectFit"
    lazy-load="{{true}}"
    binderror="onImageError"
  />
</view>