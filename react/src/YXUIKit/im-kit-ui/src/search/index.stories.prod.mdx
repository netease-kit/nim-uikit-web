import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import { AddContainer, SearchContainer } from './index'

<Meta title="IM UI Kit/搜索组件" />

# 搜索组件

搜索组件负责搜索好友或群组，并提供了添加好友、创建群组、加入群组等功能

整体由好友搜索组件和添加组件组成：

- `SearchContainer` 好友搜索组件
- `AddContainer` 添加组件

下面是对两个组件的参数说明：

## 参数说明

### SearchContainer 搜索功能组件

<ArgsTable of={SearchContainer} />

### AddContainer 添加功能组件

<ArgsTable of={AddContainer} />

## 代码示例

```tsx
import React from 'react'
import { AddContainer, SearchContainer } from '@xkit-yx/im-kit-ui'

import '@xkit-yx/im-kit-ui/es/style'

const Page = () => {
  const goChat = () => {
    // 点击去聊天，可以在这里处理路由跳转
  }

  return (
    <div
      style={{
        display: 'flex',
        width: 800,
        alignItems: 'center',
      }}
    >
      <SearchContainer onClickChat={goChat} />
      <div style={{ marginLeft: 20 }}>
        <AddContainer onClickChat={goChat} />
      </div>
    </div>
  )
}
```
